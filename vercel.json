{
  "installCommand": "cd client && npm install && cd ../server && npm install",
  "buildCommand": "npm run --prefix client build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/server" }
  ]
}
